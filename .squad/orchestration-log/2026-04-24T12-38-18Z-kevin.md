# Kevin Orchestration Log — Issue #2 Revision Batch

## Agent Status
- **Role:** Backend Dev
- **Mode:** Revision (post-review corrections)
- **Issue:** #2
- **Timestamp:** 2026-04-24T14:38:18.059+02:00

## Outcome
Corrected the rejected Indexer health probe contract from `GET /api/indexer/v2/reindex` to `GET /api/indexer/v2/readiness_check`, updated the locking tests, reran validation, and updated issue #2 with the real current status.

## Revisions Made
1. **Indexer probe contract fix:**
   - Removed mutating `GET /api/indexer/v2/reindex` endpoint
   - Replaced with read-only `GET /api/indexer/v2/readiness_check`
   - Tests updated to assert readiness endpoint (prevent accidental reversion)

2. **Health probe status semantics clarified:**
   - `healthy` = probe reached service + 2xx response
   - `unhealthy` = probe reached service + non-2xx response (includes HTTP code, error detail)
   - `error` = probe failed before response (timeout, DNS, TLS, connection; status_code=None)
   - No redirect following (redirects hide auth/gateway misconfig)
   - Deterministic result ordering per OSDU_SERVICES list

3. **EDS probe contract finalized:**
   - Probe set to `GET /api/eds/v1/health/readiness_check` (dedicated health endpoint)
   - Removed candidate `POST /api/eds/v1/retrievalInstructions` (business API, false negatives)
   - Kept EDS in issue #2 service matrix

## Decisions Recorded
- Indexer probe uses readiness endpoint (not reindex)
- EDS probe uses readiness endpoint (not retrievalInstructions)
- Health status semantics (healthy/unhealthy/error distinction)

## Tests Updated
- Locking tests re-run against readiness endpoint
- Validation re-run against full contract
- Issue #2 updated with real current status

## Status
✓ Complete — all corrections validated, ready for Judson UI integration
